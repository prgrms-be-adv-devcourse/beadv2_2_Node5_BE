server:
  port: 0

spring:
  application:
    name: subscription-service
  cloud:
    config:
      uri: http://localhost:8888
      profile: default
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always

management:
  endpoints:
    web:
      exposure:
        include: health
